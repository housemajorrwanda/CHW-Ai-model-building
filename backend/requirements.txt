# Core dependencies
sympy>=1.12

# FastAPI and server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Database
sqlalchemy>=2.0.0
alembic>=1.12.0
psycopg2-binary>=2.9.9

# Authentication
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
pyjwt>=2.8.0

# OCR
pytesseract>=0.3.10
# Optional: much better on handwritten scans (large). Enable with USE_EASYOCR=1 — not used on slim deploys.
# easyocr>=1.7.0

# Local math/handwriting models (free, no API key):
#   - microsoft/trocr-base-handwritten (English handwriting)         loaded from `transformers`
#   - fhswf/TrOCR_Math_handwritten     (handwritten formulas→LaTeX)  loaded from `transformers`
#   - pix2tex (LaTeX-OCR)              (handwritten formulas→LaTeX)  second math opinion
# The TrOCR models live in `transformers`; pix2tex is a separate package.
# pix2tex pulls torch/torchvision/timm/etc.; safe to comment out on slim deploys.
pix2tex>=0.1.4

# Cloud OCR providers (Mathpix / Google Vision / Azure Read) — used when MATH_OCR_PROVIDER + creds are set.
requests>=2.31.0

# Image processing
Pillow>=10.0.0
pdf2image>=1.17.0
pypdf>=4.0.0
pdfplumber>=0.11.0
opencv-python-headless>=4.10.0
reportlab>=4.0.0
matplotlib>=3.7.0

# Validation
pydantic>=2.0.0
pydantic[email]>=2.0.0

# Machine Learning for enhanced grading (trained on Rwanda dataset)
# Docker/Railway: Dockerfile installs requirements-railway.txt (no sentence-transformers) to stay under image size limits.
sentence-transformers>=2.2.0
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
